# Example — copy values into samba/volumes/config/accounts.env via setup.
# Real file: samba/volumes/config/accounts.env (created by samba/setup.py).
# Used by both Samba (SMB) and SFTPGo (WebDAV). Password ≠ Authentik SSO.
#
# ACCOUNT_alice=secret
# UID_alice=1000
# GROUPS_alice=1000
