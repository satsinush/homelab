# Example Samba accounts for servercontainers/samba (env_file).
# Real file: samba/volumes/config/accounts.env (created by samba/setup.py).
# Usernames should match Authentik; SMB passwords are separate.
#
# ACCOUNT_aneedham=replace-me
# UID_aneedham=1000
# GROUPS_aneedham=1000
