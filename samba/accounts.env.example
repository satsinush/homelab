# Example — real file is volumes/file-accounts/accounts.env (created by setup / dashboard).
# Shared by Samba (SMB) and SFTPGo (WebDAV). Password ≠ Authentik SSO.
#
# ACCOUNT_alice=secret
# UID_alice=1000
# GROUPS_alice=1000
