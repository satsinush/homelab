*node_modules/
.env
docker-compose.override.yml
.venv/

ddclient.conf

id_ed25519
id_ed25519.pub
*.crt
*.key
*.pem

# Shared infrastructure volumes (secrets, certificates)
volumes/

# Per-service generated volumes (gitignored universally)
*/volumes/

# Shared NAS / homes (Samba + WebDAV) — backed up by Restic
storage/

backups/

tmp/

# Local Restic exclude overrides (see .backup_exclude.example)
.backup_exclude

.vscode/*
!.vscode/settings.json

# Python caches
__pycache__/
*.pyc
*.pyo
*.pyd

# Ansible — host-specific inventory + vaulted secrets stay local
ansible/inventory.yml
ansible/vault.yml
