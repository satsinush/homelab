## 🛠️ Bug Fixes / Improvements
 * [ ] fix: Relay server unavailable Tailscale could not connect to the 'Homelab DERP' relay server. Your Internet connection might be down, or the server might be temporarily unavailable, need to make sure we have a domain that always resolves to the public IP address and maybe a different one that resolves to local?
 * [ ] fix aria hidden and other browser warnings
 * [ ] explicitly define volumes in services as an abstract function and backup/restore using those explicitly define folders instead of \*volumes
 * [ ] make sure can still scroll up whole page on mobile using inner scroll box, if results is all the way at the top and I try to keep scrolling up using touch screen it just gets stuck, also looks a bit cramped with all the nested components 

## ✨ Features
 * [ ] Setup Immich
 * [ ] Setup jellyfin
 * [ ] Add CalDAV and CardDAV
 * [ ] Use secure SMTP for email instead of unsecure SMTP relay?
 * [ ] add gitea server?
 * [ ] use proper MCP servers

## 🚀 Deployment
 * [ ] build APK for word games
 * [ ] Add git hooks (run lint, build, set version across projects) set to .githooks/
 * [ ] CI/CD to run lint and what not, single version file to set C++ frontend, api, and host api version
 * [ ] Pcie gen 3?

## 📝 Maintenance & Documentation
 * [ ] update screenshots
 * [ ] update docs
