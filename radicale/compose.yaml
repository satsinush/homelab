services:
  radicale:
    image: docker.io/tomsquest/docker-radicale:latest
    container_name: radicale
    restart: unless-stopped
    volumes:
      - ${PROJECT_ROOT}/radicale/volumes/config:/etc/radicale:ro
      - ${PROJECT_ROOT}/radicale/volumes/collections:/data/collections
    networks:
      - homelab-net
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.radicale.rule=Host(`${DAV_SERVICE_NAME}.${HOMELAB_HOSTNAME}`) && (PathPrefix(`/radicale`) || PathPrefix(`/.well-known/caldav`) || PathPrefix(`/.well-known/carddav`))"
      - "traefik.http.routers.radicale.entrypoints=websecure"
      - "traefik.http.routers.radicale.tls=true"
      - "traefik.http.routers.radicale.tls.certresolver=${TRAEFIK_CERT_RESOLVER}"
      - "traefik.http.routers.radicale.priority=20"
      - "traefik.http.routers.radicale.middlewares=radicale-slash"
      - "traefik.http.middlewares.radicale-slash.redirectregex.regex=^https?://([^/]+)/radicale$$"
      - "traefik.http.middlewares.radicale-slash.redirectregex.replacement=https://$$1/radicale/"
      - "traefik.http.middlewares.radicale-slash.redirectregex.permanent=true"
      - "traefik.http.services.radicale.loadbalancer.server.port=5232"
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:5232/.web/')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

networks:
  homelab-net:
    external: true
